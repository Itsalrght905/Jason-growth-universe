import {words} from '../data/vocabulary';import {speak} from '../utils/speech';export default function EnglishWorld({age}){return <><div className="page-intro peach-bg"><span>🦊</span><p className="eyebrow">英文乐园</p><h1>听一听，说一说</h1><p>点击小喇叭，就能听到英文读音。</p></div><div className="learning-list">{(words[Number(age)]||words[7]).map(([en,cn,emoji])=><article key={en}><span>{emoji}</span><div><h2>{en}</h2><p>{cn}</p></div><button onClick={()=>speak(en)} aria-label={`朗读 ${en}`}>🔊</button></article>)}</div></>}
