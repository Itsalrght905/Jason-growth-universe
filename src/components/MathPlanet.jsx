import {useState} from 'react';import {generateMath} from '../utils/questionGenerator';import QuestionCard from './QuestionCard';
export default function MathPlanet({state}){const [round,setRound]=useState(0);const [question,setQuestion]=useState(()=>generateMath(state.selectedAge,state.difficulty));function next(){setRound(round+1);setQuestion(generateMath(state.selectedAge,state.difficulty))}return <><div className="page-intro blue-bg"><span>🪐</span><p className="eyebrow">数学星球</p><h1>数字探险开始啦</h1><p>算一算，答案就藏在下面。</p></div><QuestionCard key={round} item={question} index={round%5} total={5} onNext={next}/></>}
